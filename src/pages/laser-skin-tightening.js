import React from "react"
import Layout from "../components/Layout"

const LaserSkinTightening = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        LaserSkinTightening
      </div>
    </Layout>
  )
}

export default LaserSkinTightening
