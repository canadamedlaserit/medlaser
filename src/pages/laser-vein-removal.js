import React from "react"
import Layout from "../components/Layout"

const LaserVeinRemoval = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        LaserVeinRemoval
      </div>
    </Layout>
  )
}

export default LaserVeinRemoval
