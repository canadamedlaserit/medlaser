import React from "react"
import Layout from "../components/Layout"

const AntiAging = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        AntiAging
      </div>
    </Layout>
  )
}

export default AntiAging
