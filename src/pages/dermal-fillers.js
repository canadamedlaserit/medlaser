import React from "react"
import Layout from "../components/Layout"

const DermalFillers = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        DermalFillers
      </div>
    </Layout>
  )
}

export default DermalFillers