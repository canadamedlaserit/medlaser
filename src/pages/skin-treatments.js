import React from "react"
import Layout from "../components/Layout"

const SkinTreatments = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        SkinTreatments
      </div>
    </Layout>
  )
}

export default SkinTreatments
