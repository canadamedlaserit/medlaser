import React from "react"
import Layout from "../components/Layout"

const Cellulite = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        Cellulite
      </div>
    </Layout>
  )
}

export default Cellulite
