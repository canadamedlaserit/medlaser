import React from "react"
import Layout from "../components/Layout"

const PermanentMakeup = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        PermanentMakeup
      </div>
    </Layout>
  )
}

export default PermanentMakeup
