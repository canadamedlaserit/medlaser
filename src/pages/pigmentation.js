import React from "react"
import Layout from "../components/Layout"

const Pigmentation = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        Pigmentation
      </div>
    </Layout>
  )
}

export default Pigmentation
