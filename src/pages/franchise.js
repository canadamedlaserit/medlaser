import React from "react"
import Layout from "../components/Layout"

const Franchise = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        Franchise
      </div>
    </Layout>
  )
}

export default Franchise