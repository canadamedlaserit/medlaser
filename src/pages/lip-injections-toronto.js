import React from "react"
import Layout from "../components/Layout"

const LipInjectionsToronto = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        LipInjectionsToronto
      </div>
    </Layout>
  )
}

export default LipInjectionsToronto