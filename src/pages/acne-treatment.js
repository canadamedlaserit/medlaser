import React from "react"
import Layout from "../components/Layout"

const Acne = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        Acne
      </div>
    </Layout>
  )
}

export default Acne
