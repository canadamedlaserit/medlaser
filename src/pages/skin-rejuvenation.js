import React from "react"
import Layout from "../components/Layout"

const SkinRejuvenation = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        SkinRejuvenation
      </div>
    </Layout>
  )
}

export default SkinRejuvenation
