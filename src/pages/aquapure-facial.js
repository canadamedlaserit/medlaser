import React from "react"
import Layout from "../components/Layout"

const AquapureFacial = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        AquapureFacial
      </div>
    </Layout>
  )
}

export default AquapureFacial
