import React from "react"
import Layout from "../components/Layout"

const EnlargedPores = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        EnlargedPores
      </div>
    </Layout>
  )
}

export default EnlargedPores
