import React from "react"
import Layout from "../components/Layout"

const AboutCML = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        AboutCML
      </div>
    </Layout>
  )
}

export default AboutCML
