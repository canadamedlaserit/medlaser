import React from "react"
import Layout from "../components/Layout"

const MoreTreatments = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        MoreTreatments
      </div>
    </Layout>
  )
}

export default MoreTreatments
