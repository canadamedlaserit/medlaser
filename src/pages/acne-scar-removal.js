import React from "react"
import Layout from "../components/Layout"

const AcneScar = ({ location }) => {
  return (
    <Layout location={location}>
      <div
        style={{
          paddingTop: "220px",
          paddingBottom: "120px",
          textAlign: "center",
        }}
      >
        AcneScar
      </div>
    </Layout>
  )
}

export default AcneScar
